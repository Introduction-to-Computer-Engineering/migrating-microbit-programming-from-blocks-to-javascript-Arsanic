input.onGesture(Gesture.Shake, function () {
    while (!(input.buttonIsPressed(Button.A))) {
        for (let i = 0; i < 2; i++) {
            music.playTone(262, music.beat(BeatFraction.Whole))
            music.playTone(262, music.beat(BeatFraction.Whole))
        }
    }
})
let id = 0
basic.forever(function () {
    for (let yid = 0; yid <= 4; yid++) {
        for (let xid = 0; xid <= 4; xid++) {
            led.plot(xid, yid)
            basic.pause(200)
            led.unplot(xid, yid)
            basic.pause(200)
        }
    }
})
